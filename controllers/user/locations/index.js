const deleteLocations = require("./deleteLocations");
const saveLocation = require("./saveLocation");
const getLocations = require("./getLocations");
const saveComparison = require("./saveComparison");

module.exports = {
    saveComparison,
    getLocations,
    deleteLocations,
    saveLocation,
};
